initSidebarItems({"enum":[["SceneError",""]],"struct":[["Scene","Scene is basically a manager for all entities and where to find them. It uses object pooling by instantiating a fixed number of entities at startup, in order to maintain a decent render speeds when creating and destoying entities during the game. Scene also provides tools for searching and borrowing spawned entities."]],"type":[["Pointer","Pointer is a reference to objects in the scene, which is used to find and update these objects. A Pointer can hold a reference to an object that doesn't exist anymore, the exists(pointer) methode can be used to check a pointer before using it."]]});